/*
 *  UCF COP3330 Fall 2021 Assignment 4 Solution
 *  Copyright 2021 Rafael Alvarez-Romero
 */

package ucf.assignments;

public class ListsFunctions {

   // protected static List<> CreateList(List<> MainList, String ListName){
        //this would be where we would start creating our lists to place in the MainList
        //no need for somewhat of a loop since we are just starting
        //start creating our list using the commands from the controller and code to put everything together
        //we can use a CSV "Excel sheet" to place every item in the list together so when its called its easier
        //we can create a obj for it, so it is easier to call back whenever we need to do something on it
        //finally place it into either the empty or populated MainList
       // return null;
    //}



    //protected static List<> EditList(List<> MainList, String ListName){
        //This here would through our lists of ToDoList and see if we can find the list we are looking for
        //Somewhat would go as i = 0; i<amount of lists; i++
        //using the MainList.get(i) would give us the object
        //use get obj to get the string name
        //copy over the obj instance to a new obj and change the name based on the ListName
        //Compare strings, if true set the MainList.Set() and then return MainList

       // return null;
    //}

    //public static List<> DestroyList(List<> MainList, String ListName){
        //Would go through all the iteration of our list of ToDoList to check where or if ListName is located
        //Somewhat would go as i = 0; i<amount of lists; i++
        //using the MainList.get(i) to give us the object
        //get the obj instance string name
        //and compare the strings, if true MainList.remove(i) and then return MainList
      //  return null;
    //}
}
